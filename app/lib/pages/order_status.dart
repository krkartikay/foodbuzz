import 'package:flutter/material.dart';

class OrderStatusPage extends StatelessWidget {
  final int oid;

  const OrderStatusPage({Key key, this.oid}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}